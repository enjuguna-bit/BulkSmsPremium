const chai = require('chai');
const expect = chai.expect;

// Billing tests have been skipped as billing feature was removed
describe.skip('Billing Functions (removed)', () => {
  it('skipped', () => {
    // keep `expect` referenced for linting
    expect(true).to.be.true;
  });
});
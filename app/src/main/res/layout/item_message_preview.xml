<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="4dp"
    app:cardBackgroundColor="@color/color_surface"
    app:cardCornerRadius="8dp"
    app:cardElevation="2dp"
    app:strokeWidth="1dp"
    app:strokeColor="@color/color_outline_variant">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="12dp">

        <!-- Recipient Info -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginBottom="8dp">

            <TextView
                android:id="@+id/txtRecipientName"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="@string/preview_recipient_name_placeholder"
                android:textSize="14sp"
                android:textStyle="bold"
                android:textColor="@color/color_on_surface" />

            <TextView
                android:id="@+id/txtPhoneNumber"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/preview_phone_number_compact_placeholder"
                android:textSize="12sp"
                android:textColor="@color/color_on_surface_variant" />

        </LinearLayout>

        <!-- Message Content -->
        <TextView
            android:id="@+id/txtMessage"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/preview_message_content_placeholder"
            android:textSize="13sp"
            android:textColor="@color/color_on_surface"
            android:lineSpacingExtra="2dp"
            android:background="@drawable/message_preview_background"
            android:padding="8dp"
            android:maxHeight="120dp" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
